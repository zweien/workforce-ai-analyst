# WorkForce AI 考勤分析

面向 HR/管理者的考勤工时分析工具:上传考勤 Excel,本地生成统计与可视化,可选用 LLM 生成分析报告。桌面端为 Windows 优先的跨平台应用。

## Language

### 数据

**考勤记录**:
一条原始数据行:某员工在某批次的一条工作记录,含姓名、部门、职位、批次、工时。
_Avoid_: 打卡记录、流水

**批次**:
一个考勤统计周期(按月)。多个文件可混合上传,每个文件对应一个批次。
_Avoid_: 月份(界面文案需统一为"批次")

**工时**:
一条考勤记录中的工作时长(小时数)。
_Avoid_: 时长、小时数

**部门** / **职位**:
员工的组织归属;是筛选维度和聚合维度。

**汇总**:
按人(姓名+部门+职位)跨所选批次聚合后的结果:总工时、记录数。
_Avoid_: 明细(明细指聚合前的考勤记录)

### 分析

**本地统计**:
在用户设备上由程序直接计算的概览卡片、图表与汇总表。不依赖任何 LLM,始终可用。
_Avoid_: 离线模式(它不是降级态,是常态能力)

**AI 分析**:
用户点击按钮后,由 LLM 基于筛选后的汇总数据生成的 Markdown 报告。显式触发,永不自动运行。
_Avoid_: 智能报告、自动分析

**脱敏**:
数据离开设备前,将员工姓名替换为"员工A/B…"等代称,部门与职位保留。默认开启。
_Avoid_: 匿名化、隐私处理

**高负荷名单**:
按工时排序的前 5 名员工,是 AI 分析数据中唯一的个人级信息,也是脱敏的作用对象。
